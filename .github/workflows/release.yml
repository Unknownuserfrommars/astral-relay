name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    name: Build, verify and publish GitHub Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Full quality chain
        run: |
          npm run typecheck
          npm run build
          npm test
          npm run check-sync

      - name: Verify tag matches manifest version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          MANIFEST_VERSION=$(node -e "console.log(require('./manifest.json').version)")
          PKG_VERSION=$(node -e "console.log(require('./package.json').version)")
          if [ "$TAG" != "$MANIFEST_VERSION" ] || [ "$TAG" != "$PKG_VERSION" ]; then
            echo "::error::tag v$TAG != manifest $MANIFEST_VERSION / package $PKG_VERSION"
            exit 1
          fi

      - name: Package plugin ZIP
        run: |
          cd dist/plugin
          zip -r "../astral-relay-${GITHUB_REF_NAME}.zip" astral-relay
          cd ../..
          unzip -l "dist/astral-relay-${GITHUB_REF_NAME}.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/astral-relay-*.zip
          generate_release_notes: true
