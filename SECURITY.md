# Security Policy

## 信任边界（先读这段）

Cyrene 插件运行在 **Electron 主进程，拥有完整 Node 权限**——不是沙箱，不是
浏览器扩展，没有权限隔离。这是宿主写明的信任模型：只安装你能审查且信任来源的插件。

在这个模型内，本插件尽量压低自己的暴露面：

- 只申请 `secrets` 一项宿主能力，用于加密保存订阅 Key
- **零运行时依赖**：只用 Node 内置模块与宿主注入的服务
- 不读写自己插件数据目录之外的任何文件
- 本地代理**只绑 127.0.0.1**，端口由内核分配，并要求随机 token
- 出网目标只有用户自己选定的那一个套餐官方端点，不含遥测与第三方回传

## 订阅 Key 的处理

| 环节 | 做法 |
|---|---|
| 存储 | 只经 `ctx.deps.secrets`，按插件命名空间隔离 |
| 日志 | 任何日志点都不打印 Key；上游失败只打印错误消息与套餐名 |
| IPC | `get-state` 只回传 `keyConfigured` 布尔值，不回传 Key |
| 面板 | 输入框是 `type="password"`，保存后立即清空，不回填 |

契约测试里有一条专门钉这件事：把 Key 存进去后，`get-state` 的整个返回值
序列化后不得包含 Key 的任何片段。

## 闸门的安全属性

闸门是本插件的合规执行点，威胁模型是「防止无人值守路径消耗套餐额度」，
不是「防止本机恶意进程」——后者在插件的信任模型下本来就不成立。

已知弱点已在 README「已知限制」中如实写出：闸门是时间窗而非逐请求绑定。
请不要把它理解为强隔离。

## 报告漏洞

请通过 GitHub Security Advisory 私下报告，不要开公开 issue。
