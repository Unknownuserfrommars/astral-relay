# Security Policy

## 信任边界（先读这段）

Cyrene 插件运行在 **Electron 主进程，拥有完整 Node 权限**——不是沙箱，不是
浏览器扩展，没有权限隔离。这是宿主写明的信任模型：只安装你能审查且信任来源的插件。

在这个模型内，星驿尽量压低自己的暴露面：

- 只申请 `secrets` 一项宿主能力，用于加密保存订阅 Key 与 OAuth 凭据
- **零运行时依赖**：只用 Node 内置模块与宿主注入的服务
- 不读写自己插件数据目录之外的任何文件
- 本地代理**只绑 127.0.0.1**，端口由内核分配，并要求随机 token
- 出网包含所选厂商的授权、token、模型目录和推理端点；统一使用 Electron `net.fetch()`，不含遥测

## 订阅 Key 的处理

| 环节 | 做法 |
|---|---|
| 存储 | 只经 `ctx.deps.secrets`，按插件命名空间隔离，并按厂商分键 |
| 日志 | 任何日志点都不打印 Key；上游失败只打印错误消息与厂商 id |
| IPC | `get-state` 只回传每家的 `keyConfigured` 布尔值，不回传 Key |
| 面板 | 输入框是 `type="password"`，保存后立即清空，不回填 |

契约测试里有一条专门钉这件事：把 Key 存进去后，`get-state` 的整个返回值
序列化后不得包含 Key 的任何片段。

## OAuth 凭据

Grok 使用 PKCE + 随机 state，本地回调仅绑定 `127.0.0.1`，先监听再打开浏览器；设备码流程不开本地端口。
错误 state 不会结束有效登录，取消、5 分钟超时或插件停用会清理监听器。token 请求另有 30 秒超时。
OAuth 凭据只存入宿主 `secrets`；安全存储不可用时拒绝连接，不回退到明文。
面板仅获取 connected / connecting / expiresAt 状态，不获取 access token、refresh token、授权码或账号标识。
同厂商并发刷新合并为一次；断开连接后不会被迟到的刷新重新写回凭据。

网络目标包括 `auth.openai.com`、`chatgpt.com`、`auth.x.ai` 和 `api.x.ai`。
HTTP token / 推理请求禁止重定向，避免授权头转发到意外目标。原始授权错误响应不进入日志或面板。

## 模式验证的安全属性

模式验证是编程套餐类厂商的合规执行点，威胁模型是「防止无人值守路径消耗订阅额度」，
不是「防止本机恶意进程」——后者在插件的信任模型下本来就不成立。

只约束 `kind === "coding-only"` 的厂商。通用厂商（条款没有把场景限定在编程工具的）
不受限制，这是刻意的：没有依据的限制只是功能阉割。

判定有两条路径，都不需要宿主改动之外的任何东西：

1. **Code 轮次绑定（默认，自包含）**：prompt provider 只在 Code 模式的交互式会话被调用
   （插件自己再核对一次 mode / source），登记该轮用户输入的 SHA-256 指纹；
   代理核对请求体里最后一条 user 消息的指纹。**做得到**：拒绝定时任务、发帖决策、
   连接测试与 Chat / Work / Learn。**做不到**：区分「同一段文本在 Code 之后于其它模式重发」，
   以及隔离持有本地 token 的本机程序——它重放同样的文本即可通过。
2. **`ar1` 签名凭据（可选，需宿主配合）**：逐请求验证宿主签名的厂商、模式、来源与签发时间。
   带签名时以签名为准，不回退到内容匹配，因此路径 1 的第一个缺口不存在。
   它同样不隔离本机恶意进程——那个程序可以用本地 token 自行签名。

面板上的 Code 活动指示器不参与任何授权判定。登记只存指纹不存原文，
`get-state` 只回传条目数量。插件停用即清空全部登记。

## 代理的鉴权

通用套餐接受随机 token 或有效的 HMAC-SHA256 模式凭据；
编程套餐要么是有效的 Code 会话签名凭据，要么内容命中 Code 轮次登记。
签名按固定长度比较，凭据不可跨厂商使用，最长有效 24 小时，
且 nonce 自首次使用起 2 小时内有效——不做「只许用一次」，
因为同一轮的流式重试与工具续轮共用同一个凭据，严格去重会打断工具续轮。
代理转发前将本地凭据替换为订阅 Key 或 OAuth access token。

## 报告漏洞

请通过 GitHub Security Advisory 私下报告，不要开公开 issue。
